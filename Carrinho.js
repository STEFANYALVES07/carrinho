import { Text } from "react-native-web";

export default function Carrinho() {
  return;
  <Text>teeeee</Text>;
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
  },
});
